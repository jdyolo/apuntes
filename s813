using UnityEngine;

public class gamemanager : MonoBehaviour
{
    void Start()
    {
        for (int i = 1; i <= 20; i++)
        {
            if (i % 2 == 0)
            {
                continue;
            }

            Debug.Log(i);
        }
    }
}
